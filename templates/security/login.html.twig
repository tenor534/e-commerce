{% extends 'base.html.twig' %}

{% block title %}Login{% endblock %}

{% block body %}

<div class="archive-title clearfix">
    <div class="row align-items-end">

        <div class="col-md-8">
            <h2 class="page-title mb-2 mb-md-0">Login</h2>
            {#<span class="fs-13 text-gray-soft">Category</span>#}            
        </div>
    </div>

</div>

<div class="bd-callout bd-callout-info">
    Si vous vous mettez à écrire des lignes de code au fur et à mesure, ça va être à coup sûr le bazar. À l'inverse, si vous prenez cinq minutes pour y réfléchir devant une feuille de papier, votre code sera mieux structuré et vous éviterez de nombreuses erreurs (qui font, elles, perdre du temps).
</div>

 <div>
    <!-- Content here-->
    <section>
        <div class="bd-exemple-snippet bd-code-snippet">
            <div class="bd-example m-0 border-0">               

                <form method="post">
                    <section>
                        <div class="container">
                            <div class="row">
                                <div class="col-12 d-flex justify-content-center mt-0">
                                    <img src="https://themes.getbootstrap.com/wp-content/themes/bootstrap-marketplace/assets/images/elements/bootstrap-stack.png" alt="logo" class="rounded mx-auto d-block" width="107" height="99">
                                </div>
                                <div class="col-12 d-flex justify-content-center mt-5">
                                    <div class="card border-0 shadow p-3 mb-5 bg-body-tertiary rounded" style="width: 40rem;">
                                        <div class="card-header bg-body-tertiary border-0 text-center">
                                            <h1>Connexion</h1>
                                        </div>
                                        <div class="card-body">
                                            {% if error %}
                                                <!--div class="alert alert-danger">Identifiant incorrect</div-->
                                                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                                            {% endif %}
                                            {% if app.user %}
                                                <div class="mb-3">
                                                    You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_main') }}">Home</a>
                                                </div>
                                            {% endif %}
                                            <form action="">
                                                <label for="inputEmail">Email</label>
                                                <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
                                                <label for="inputPassword" class="mt-3">Password</label>
                                                <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
                
                                                <input type="hidden" name="_csrf_token"
                                                       value="{{ csrf_token('authenticate') }}"
                                                >
                                                <p class="mt-3">Pas de compte ? <a href="{{ path('app_register') }}">Register</a></p>
                                                <p class="mt-3"><a href="{{ path('connect_google') }}">Se connecter avec Google ?</a></p>
                                                
                                                <button class="btn btn-lg btn-primary mt-3" type="submit">
                                                    Sign in
                                                </button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </section>
                </form>
            </div>
        </div>
    </section>
</div>   

{% endblock %}
